@using NewLife.Web;
@using NewLife.Cube;
@using XCode.Membership;
@{
    var page = ViewBag.Page as Pager;
    var DepartmentList = Department.FindAll();
}
<div class="form-group">
    <label for="DepartmentFor" class="control-label">企业名称：</label>
    @Html.ForDropDownList("Department", DepartmentList, page["Department"], "全部", true)
</div>
